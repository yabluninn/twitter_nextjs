import { ITweet } from "@/src/shared/types/tweet.interface";

interface Props {
  tweet: ITweet;
}

export default function Tweet({}: Props) {
  return (
    <div>
      <div></div>
    </div>
  );
}
